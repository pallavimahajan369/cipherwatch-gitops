apiVersion: v1
kind: Service

metadata:
  name: cipherwatch
  namespace: apps

spec:
  selector:
    app: cipherwatch

  ports:
    - name: http
      port: 80
      targetPort: 8080

  type: ClusterIP
